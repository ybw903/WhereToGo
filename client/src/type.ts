export type TourData = {
    title: string
    content: string
}

export type ImageInfo = {
    imgUrl: string
    title: string
}

export type ScheduleInfo = {
    id : number
    startTime: string
    endTime: string
    place: any
    expense: number
    memo: string
}