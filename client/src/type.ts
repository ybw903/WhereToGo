export type TourData = {
    title: string
    content: string
}

export type ImageInfo = {
    imgUrl: string
    title: string
}

export type ScheduleInfo = {
    id : number
    startTime: string
    endTime: string
    place: string
    fee: number
    memo: string
}